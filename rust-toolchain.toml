[toolchain]
channel = "1.83.0"          # pinned exact; bump deliberately, record in CHANGELOG
components = ["clippy", "rustfmt"]
# rust-src + linux targets omitted for local macOS dev (rustup component
# conflict on existing installs). CI/docker pin their own.
